server {
    listen 8080 default_server;
    server_name 'wiki.osuosl.org';

    location / {
        root <%= ::File.join(node['osl-testcookbook']['wiki-webroot'], "build/html")%>;
        index index.html;
    }
}
